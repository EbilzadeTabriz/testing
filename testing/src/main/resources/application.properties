#server port we can change this point easily
# if we want to create our application in different localhost
server.port=8080

# MySQL database connection
spring.datasource.url=jdbc:mysql://localhost:3306/emp_controller
spring.datasource.username=root
spring.datasource.password=Kenan2004
spring.datasource.driverClassName=com.mysql.cj.jdbc.Driver

# Hibernate settings for MySQL
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true